PUBMED_EMAIL=your_email@example.com
PUBMED_API_KEY=your_pubmed_api_key

PUBMED_EUTILS_LIMIT=10 # Set the PubMed,requests/second limit. Default is 10, which is the maximum allowed by NCBI. Adjust as needed.

MCP_HTTP_PATH=/pubmed-mcp
MCP_BEARER_TOKENS=your-mcp-token-1,your-mcp-token-2